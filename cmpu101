REGISTERS #######################################################################################################################################
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
.intel_syntax noprefix
.global _start                  ; as we see rdi is used to mov a value of mem address to his self 
_start:
 mov rdi,[rdi]
 mov rax,60
 syscall
+++++++++++++++++++++++++++++++

if a register point to an array -> her it comes the term OFFSET 
if we want access the second index for exemple we do [rdi+1]    [rdi]=value of 1st index

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Similarly, since memory addresses are really just values, they can be stored in memory, and retrieved later.
a scenario where we store the value 133700 at the address 123400, and store the value 42 at the address 133700. Consider the following instructions:

mov rdi, 123400    # after this, rdi becomes 123400
mov rdi, [rdi]     # after this, rdi becomes the value stored at 123400 (which is 133700)
mov rax, [rdi]     # here we dereference rdi, reading 42 into rax!























#################################################################################################################################################
STACK 
-------------------------------------------------------------------------------------------------------------------------------------------
sub rsp,8                    |
mov [rsp],0x1245654589788587 |--------------> equivalent to mov rax,0x1245654589788587  -> push rax OR you can push it direclty push 0x1245654589788587



======>La Pile (Gestion Système) : Grandit vers le bas (les adresses basses) uniquement quand le système réserve de 
l'espace pour une nouvelle fonction ou fait un PUSH.

======>Les Données (Écriture) : S'écrivent toujours vers le haut (les adresses hautes), du début du tableau vers la fin.















----------------------------------------------------------------------------------------------------------------------------------------------
