    ;  Reach out to RAM to READ the value into the CPU
    mov rax, [ramUsageBuffer + 40]  ; Brackets used: reading from RAM

    ;  Manipulate the value directly INSIDE the CPU chip
    add rax, 48                     ; No brackets: modifying the register directly

    ; Reach out to RAM to WRITE the value back down
    mov [tempBufferNumber], rax     ; Brackets used--->writing back into RAM


   lea -> loads a memory address but it needs to use [r registers like r11,r12..]
   inc -> do add one byte to the mem address loaded with lea EXP: inc r11
   cmp -> we do compare registers | but we do not compare memory alocated variables EXP: cmp rax,rbx is good          
          but cmp hello,hi     WILL cCRAsh
